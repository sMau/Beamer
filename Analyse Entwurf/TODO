Bug

Themeslidecreator.java - Erst Schriftgröße dann Schriftart gewählt führte zu reset der Größe evtl. auch umgekehrt


Feature (Backend)

Netzwerkschnittstelle, trenne Programm in Server und Client
	Multiuserbetrieb im Netzwerk mit "Login"
	Teilnehmer Beamer Interaktion (über Webpage) kleiner bereich wo diese posts auf dfem beamer angezeigt werden (vll wie liveticker)
mark as showed only after a given time (with a swing timer) (configurable via prefs  /props)
Themeslide editing
(?) Themeslidecreator - on pressing enter add the current tabspace automatically (like in eclipse)
(?) Themeslidecreator - Zooming in and out in creation mode
add a "clear button" espacially to stop a countdown and fall back to blank screen (or the background)
Adding ticket elements without dialog, instead simply add ticket elt with "showing false" text "new element"



Design / GUI

entwürfe in leiste (schnellzugrtiff) bereits erstellte folien werden da gesammelt (z.b. alle oder via haken einstellbar)
Layouts anstatt freiem design bneim folien erstellen
	layouts selbst erstellen (ähnlich wie bei powerpoint sind layouts zu verstehen)
preview liveticker and countdown font directly on beamer (after setting show a piece of text with given paras) (maybe via interface previewable)
"show file at" Gui mapping and testing
"big" preview window as "popup" own frame (maybe)
Keylistener in espacially in the tables
right click context menus, extending for changeprio and change name, and so on
"mouse resizable" preview window
move up down via drag n drop
Move to Top move to BOttom methods
Filemanger Table model in case 0 und 1 sollten Icons gerendert werden anstatt Textzeichen (playstatus)



Ideas

Android App - für Netzwerksteuerung
Theme also contains the margin and fontsize/type presettings (can contain there are also overall default settings in the settings dialogs)
Queing for media files (like spotify does)
provide countdown with text around
after themeslide creation suggest to add a ticker elt.
enable disable funktion in medien tabelle, wie beim live ticker




Optimization (Design / Performance)

Improve properties architecture. Espacially using the preferenceshandler class to handle all the write and read ops to the props object.
Classes which want a property only ask the preferenceHandler instead of handling the props reading ops themselves
Make a clean startup, löst bestimmt manche abhängigkeiten usw und ermöglicht ladebalken und so (assure clean loading (force all singletons to create instances and so on) (including a loading bar)
performance improve on building chess pattern in background, maybe use clip or copyarea to do so
Performance fix on changing high res images. shows for a sec a blank screen
Check every pointer to file system and make them save for moving files on the filesystem, then warn the user that a file isnt available anymore
Postioning of dialogs not mousespecific but parent centered



Clean Up / Refactoring

ALL Classes and Packages
add logging in the complete program
check if everywhere bufferedImage with ImageIO is used







 

